# Changelog

## 0.1.0

Initial registry-ready packaging baseline for the bundled MCP App Viewer companion extension.

Highlights:

- URI-handler bridge from the CLI to VS Code-family Simple Browser commands
- loopback-only HTTP safety checks for handler requests
- editor-group creation and reuse so repeated renders replace the app in place
- packaging metadata and VSIX build support for local registry validation
